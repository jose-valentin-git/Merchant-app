import { FC } from "react";
import LocationModal from "./locationcomponent/LocationModal";

const LocationButton: FC = () => {
  return (
    <>
      <LocationModal ></LocationModal>
    </>
  );
};

export default LocationButton;
